import React from 'react';

import './styles.scss';

import Header from '../Header';

const Portfolio = () => {
  return (
    <div className="portfolio">
      <Header />
    </div>
  );
};

export default Portfolio;